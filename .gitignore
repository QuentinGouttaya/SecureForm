# ============================================================
# RUST / CARGO (backend/)
# ============================================================
debug/
target/
**/*.rs.bk
*.pdb
**/mutants.out*/

# SQLx offline query cache (commit si tu veux le check compile-time en CI sans DB)
# .sqlx/   <-- décommenter pour l'exclure du repo

# ============================================================
# NODE.JS / SVELTE 5 / SVELTEKIT (frontend/)
# ============================================================
node_modules/
dist/
build/
.svelte-kit/
.vercel/
.netlify/
.cloudflare/
.turbo/

# Lockfiles alternatifs (garde uniquement celui que tu utilises)
package-lock.json
yarn.lock
# pnpm-lock.yaml  <-- commenter si tu utilises pnpm

# Vite timestamps
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright / Vitest
test-results/
playwright-report/
coverage/

# ============================================================
# VARIABLES D'ENVIRONNEMENT & SECRETS (CRITIQUE RGPD/NIS2)
# ============================================================
.env
.env.*
!.env.example       # Garder le template versionné
!.env.test          # Optionnel : si tu veux versionner les vars de test

# Clés AES, secrets JWT, HMAC — jamais en clair dans le repo
*.pem
*.key
*.p12
*.pfx
secrets/
keys/

# ============================================================
# DOCKER & INFRA
# ============================================================
# Volumes PostgreSQL locaux
infra/postgres-data/
infra/pgdata/

# Override Docker Compose avec secrets locaux
docker-compose.override.yml

# ============================================================
# ÉDITEURS & OS
# ============================================================
.DS_Store
Thumbs.db
.idea/
.vscode/settings.json     # Garder .vscode/extensions.json si tu veux
*.swp
*.swo
*~

# ============================================================
# LOGS & DIVERS
# ============================================================
*.log
logs/
*.pid
.pnpm-debug.log

# ============================================================
# SÉCURITÉ / CONFORMITÉ (spécifique à ce projet)
# ============================================================
# DPIA draft non finalisé — à versionner uniquement en version finale
docs/dpia-draft*.md

# Rapports de pentest
pentest-reports/
